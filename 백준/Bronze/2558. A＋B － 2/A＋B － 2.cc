#include <iostream>
#include <string>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
     
	int a, b, sum;

    cin >> a >> b;
    sum = a + b;
    cout << sum;

    return 0;
}
