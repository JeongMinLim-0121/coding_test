#include <string>
#include <vector>
#include <iostream>
using namespace std;

vector<int> solution(vector<int> num_list, int n) {
    vector<int> answer;
    
 
    return vector<int> (num_list.begin(), num_list.begin()+n);
}